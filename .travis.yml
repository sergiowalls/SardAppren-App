language: android
jdk: openjdk12

android:
  components:
    - build-tools-28.0.3
    - android-28

before_install:
  - nvm install "10.15"
  - npm install -g react-native-cli

script:
  - cd android
  - ./gradlew clean
  - ./gradlew assemblDebug